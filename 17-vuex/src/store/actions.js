/*
  This is a separate Actions file for actions that need to be "centralized"
 */

export const updateValue = ({commit}, payload) => {
  commit('updateValue', payload );
};

//export const action2 = () => {};
